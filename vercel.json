{
  "version": 2,
  "installCommand": "npm install --omit=dev --ignore-scripts",
  "functions": {
    "api/*.js": {
      "maxDuration": 300,
      "memory": 1024
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ],
  "crons": [
    {
      "path": "/checkin",
      "schedule": "0 1 * * *"
    }
  ]
}
